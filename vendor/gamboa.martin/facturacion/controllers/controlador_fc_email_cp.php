<?php
/**
 * @author Martin Gamboa Vazquez
 * @version 1.0.0
 * @created 2022-05-14
 * @final En proceso
 *
 */
namespace gamboamartin\facturacion\controllers;

use gamboamartin\cat_sat\models\cat_sat_motivo_cancelacion;
use gamboamartin\errores\errores;
use gamboamartin\facturacion\html\fc_complemento_pago_html;
use gamboamartin\facturacion\html\fc_email_cp_html;
use gamboamartin\facturacion\models\fc_complemento_pago;
use gamboamartin\facturacion\models\fc_email_cp;
use gamboamartin\system\links_menu;
use gamboamartin\system\system;
use gamboamartin\template\html;
use html\com_email_cte_html;
use PDO;
use stdClass;

class controlador_fc_email_cp extends system{

    public array|stdClass $keys_selects = array();



    public function __construct(PDO $link, html $html = new \gamboamartin\template_1\html(),
                                stdClass $paths_conf = new stdClass()){
        $modelo = new fc_email_cp(link: $link);
        $html_ = new fc_email_cp_html(html: $html);
        $obj_link = new links_menu(link: $link, registro_id:  $this->registro_id);


        $datatables = $this->init_datatable();
        if(errores::$error){
            $error = $this->errores->error(mensaje: 'Error al inicializar datatable',data: $datatables);
            print_r($error);
            die('Error');
        }

        parent::__construct(html:$html_, link: $link,modelo:  $modelo, obj_link: $obj_link, datatables: $datatables,
            paths_conf: $paths_conf);


        $this->lista_get_data = true;

        $this->parents_verifica[] = (new fc_complemento_pago(link: $this->link));
        $this->parents_verifica[] = (new cat_sat_motivo_cancelacion(link: $this->link));


        $this->verifica_parents_alta = false;

    }


    final public function init_datatable(): stdClass
    {

        $columns["fc_complemento_pago_folio"]["titulo"] = "Fol";
        $columns["com_email_cte_descripcion"]["titulo"] = "Correo";
        $columns["com_cliente_rfc"]["titulo"] = "Cte";

        $filtro = array("fc_complemento_pago.folio","com_email_cte.descripcion", "com_cliente.rfc");

        $datatables = new stdClass();
        $datatables->columns = $columns;
        $datatables->filtro = $filtro;

        return $datatables;
    }

    public function alta(bool $header, bool $ws = false): array|string
    {
        $r_alta =  parent::alta($header, $ws); // TODO: Change the autogenerated stub
        if(errores::$error){
            return $this->retorno_error(mensaje: 'Error al insertar',data:  $r_alta,header:  $header,ws:  $ws);
        }
        $fc_complemento_pago_id = (new fc_complemento_pago_html(html: $this->html_base))->select_fc_complemento_pago_id(cols:12,
            con_registros:  true,id_selected: -1,link: $this->link);
        if(errores::$error){
            return $this->retorno_error(mensaje: 'Error al generar input',data:  $fc_complemento_pago_id,header:  $header,ws:  $ws);
        }

        $com_email_cte_id = (new com_email_cte_html(html: $this->html_base))->select_com_email_cte_id(cols:12,
            con_registros:  true,id_selected: -1,link: $this->link);
        if(errores::$error){
            return $this->retorno_error(mensaje: 'Error al generar input',data:  $com_email_cte_id,header:  $header,ws:  $ws);
        }

        $this->inputs = new stdClass();
        $this->inputs->fc_complemento_pago_id = $fc_complemento_pago_id;
        $this->inputs->com_email_cte_id = $com_email_cte_id;
        return $r_alta;
    }

    public function modifica(bool $header, bool $ws = false): array|stdClass
    {
        $r_modifica = parent::modifica($header, $ws); // TODO: Change the autogenerated stub
        if(errores::$error){
            return $this->retorno_error(mensaje: 'Error al generar template',data:  $r_modifica,header:  $header,ws:  $ws);
        }
        $fc_complemento_pago_id = (new fc_complemento_pago_html(html: $this->html_base))->select_fc_complemento_pago_id(cols:12,
            con_registros:  true,id_selected: $this->row_upd->fc_complemento_pago_id,link: $this->link);
        if(errores::$error){
            return $this->retorno_error(mensaje: 'Error al generar input',data:  $fc_complemento_pago_id,header:  $header,ws:  $ws);
        }

        $com_email_cte_id = (new com_email_cte_html(html: $this->html_base))->select_com_email_cte_id(cols:12,
            con_registros:  true,id_selected: $this->row_upd->com_email_cte_id,link: $this->link);
        if(errores::$error){
            return $this->retorno_error(mensaje: 'Error al generar input',data:  $com_email_cte_id,header:  $header,ws:  $ws);
        }

        $this->inputs = new stdClass();
        $this->inputs->fc_complemento_pago_id = $fc_complemento_pago_id;
        $this->inputs->com_email_cte_id = $com_email_cte_id;

        return $r_modifica;
    }


}
